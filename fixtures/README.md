# Fixtures

Fixture data for unit and integration tests. Keep entries small, deterministic
and committed to version control so tests stay reproducible.

Typical contents:

- Sample input files or arguments fed to the CLI.
- Expected stdout / stderr used for assertions.
- Golden files for snapshot testing of command output.
